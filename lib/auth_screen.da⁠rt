import 'package:flutter/material.dart';
import 'home_screen.dart'; // تأكد إن هذا هو اسم ملف الشاشة الرئيسية عندك أو استبدله بالصفحة المناسبة

class AuthScreen extends StatelessWidget {
  const AuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFF0F0728), // خلفية مظلمة فخمة تناسب أجواء البث
      body: Padding(
        padding: const EdgeInsets.all(24.0),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            // شعار أو أيقونة التطبيق
            const Icon(
              Icons.live_tv_rounded,
              size: 80,
              color: Colors.purpleAccent,
            ),
            const SizedBox(height: 20),
            const Text(
              "مرحباً بك في Dodi Live",
              textAlign: TextAlign.center,
              style: TextStyle(
                color: Colors.white,
                fontSize: 26,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 10),
            const Text(
              "انطلق في عالم البث المباشر وتفاعل مع الأصدقاء",
              textAlign: TextAlign.center,
              style: TextStyle(
                color: Colors.white60,
                fontSize: 14,
              ),
            ),
            const SizedBox(height: 50),
            
            // حقل رقم الهاتف أو البريد (كمثال واجهة)
            TextField(
              style: const TextStyle(color: Colors.white),
              decoration: InputDecoration(
                filled: true,
                fillColor: Colors.white10,
                hintText: 'أدخل بريدك الإلكتروني أو رقم الهاتف',
                hintStyle: const TextStyle(color: Colors.white38),
                prefixIcon: const Icon(Icons.person, color: Colors.purpleAccent),
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(12),
                  borderSide: BorderSide.none,
                ),
              ),
            ),
            const SizedBox(height: 20),
            
            // زر تسجيل الدخول
            ElevatedButton(
              style: ElevatedButton.styleFrom(
                backgroundColor: Colors.deepPurple,
                foregroundColor: Colors.white,
                padding: const EdgeInsets.symmetric(vertical: 16),
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
              ),
              onPressed: () {
                // الانتقال للشاشة الرئيسية بعد تسجيل الدخول
                Navigator.pushReplacement(
                  context,
                  MaterialPageRoute(builder: (context) => const HomeScreen()), // استبدل HomeScreen بالصفحة الرئيسية عندك لو مسمى مختلف
                );
              },
              child: const Text(
                'تسجيل الدخول / متابعة',
                style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
